#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-image-embed/bin/grunt-image-embed" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-image-embed/bin/grunt-image-embed" "$@"
  ret=$?
fi
exit $ret
